<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/secondaryLightColor"
    tools:context=".exercises.SplashPerceptionFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/textViewInstPerception"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:gravity="center"
        android:text="Selecciona cúal imagen es \nexactamente igual a la de la izquierda."
        android:textAlignment="center"
        android:textSize="70sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"/>

    <ImageButton
        android:id="@+id/perceptionButtonInst"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/textViewInstPerception"
        android:src="@mipmap/volume_relax"
        android:background="@android:color/transparent"/>

    <Button
        android:id="@+id/buttonStartPerception"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="25dp"
        android:layout_below="@+id/perceptionButtonInst"
        android:backgroundTint="@color/bg_login"
        android:layout_gravity="center"
        android:layout_centerHorizontal="true"
        android:text="EMPEZAR"
        android:padding="20dp"
        android:textSize="40sp"
        android:textColor="@color/primaryTextColor"
        android:textStyle="bold"/>

</RelativeLayout>